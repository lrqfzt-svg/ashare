__pycache__/
*.pyc
# 网页版研判报告（Claw make_report.py 产出，绝不进 ashare）
收盘复盘与明日研判_*.html
收盘复盘与明日研判_*.pdf

# 注：模板版历史归档（reports/）与 archive.json 需入库，供 Cloudflare 站点展示历史。
# 旧的「收盘复盘模板版_*.html」散落文件已不再生成，保留排除以防残留。
收盘复盘模板版_*.html

# 本地假数据/测试残留备份（宁缺毋假清理时 mv 出来，绝不入库）
_trash/

# 采集层临时探测/日志/中间产物（collect.py / mx-data 运行残留，绝不入库）
_*/
mx_data_*
mx_search_*

# 源码入库说明：网站程序全部 .py 源码入库（collect_westock.py / update.py /
# gen_template.py / collect.py / xcheck.py / make_template.py），保证可复现。
# 仅忽略编译产物与本地临时/假数据：
#   *.pyc / __pycache__  —— 编译缓存
#   _*/  _trash/  mx_data_*  mx_search_*  —— 本地探测/日志/中间产物（绝不入库）
#   _bench/  —— 本地基准测试残留（如需要可取消此行）
_bench/

